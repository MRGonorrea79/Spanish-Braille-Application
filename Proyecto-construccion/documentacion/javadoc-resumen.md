# JavaDoc - resumen

Generar JavaDoc:
```cmd
mvn javadoc:javadoc
```

Salida: `target/site/apidocs/`.

Resumen de clases clave:
- `BrailleMapper`: servicio responsable de convertir texto a la representación Braille.
- `TranscriptionController`: controlador que expone la interfaz web para recibir texto y mostrar el resultado.

Incluye ejemplos de llamadas internas (si procede) y referencia a paquetes.
