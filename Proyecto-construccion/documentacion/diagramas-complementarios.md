# Diagramas Complementarios - Diseño Arquitectónico

Este documento complementa el diseño arquitectónico de alto nivel con diagramas de secuencia y componentes adicionales.

## 📁 Diagramas PlantUML

Todos los diagramas de este documento están disponibles en formato PlantUML editable en la carpeta:
**`diagrams/`**

### Archivos PlantUML Disponibles:
1. `01-componentes-dependencias.puml` - Diagrama de Componentes
2. `02-flujo-datos-braillemapper.puml` - Flujo de Datos
3. `03-estructura-datos-braille.puml` - Estructura de Datos
4. `04-arquitectura-capas.puml` - Arquitectura de Capas
5. `05-patrones-diseno.puml` - Patrones de Diseño
6. `06-clase-detallado.puml` - Diagrama de Clases Detallado

## 1. Diagrama de Secuencia - Flujo de Transcripción Completo

```
┌─────────┐         ┌──────────────┐         ┌─────────────────────┐         ┌──────────────┐         ┌──────────┐
│ Usuario │         │   Navegador  │         │ TranscriptionController│       │ BrailleMapper│         │ Thymeleaf│
└────┬────┘         └──────┬───────┘         └──────────┬──────────┘         └──────┬───────┘         └────┬─────┘
     │                     │                             │                           │                      │
     │  1. Accede a "/"    │                             │                           │                      │
     │────────────────────>│                             │                           │                      │
     │                     │                             │                           │                      │
     │                     │  2. GET /                   │                           │                      │
     │                     │────────────────────────────>│                           │                      │
     │                     │                             │                           │                      │
     │                     │  3. Retorna "index"         │                           │                      │
     │                     │<────────────────────────────│                           │                      │
     │                     │                             │                           │                      │
     │                     │  4. Solicita template       │                           │                      │
     │                     │─────────────────────────────────────────────────────────────────────────────>│
     │                     │                             │                           │                      │
     │                     │  5. index.html renderizado  │                           │                      │
     │                     │<─────────────────────────────────────────────────────────────────────────────│
     │                     │                             │                           │                      │
     │  6. Página cargada  │                             │                           │                      │
     │<────────────────────│                             │                           │                      │
     │                     │                             │                           │                      │
     │  7. Ingresa texto   │                             │                           │                      │
     │  "Hola 123"         │                             │                           │                      │
     │────────────────────>│                             │                           │                      │
     │                     │                             │                           │                      │
     │  8. Submit form     │                             │                           │                      │
     │────────────────────>│                             │                           │                      │
     │                     │                             │                           │                      │
     │                     │  9. POST /transcribir       │                           │                      │
     │                     │     texto="Hola 123"        │                           │                      │
     │                     │────────────────────────────>│                           │                      │
     │                     │                             │                           │                      │
     │                     │                             │  10. transcribir("Hola 123")                    │
     │                     │                             │──────────────────────────>│                      │
     │                     │                             │                           │                      │
     │                     │                             │                           │  11. Procesa:        │
     │                     │                             │                           │      - H → ⠠⠓       │
     │                     │                             │                           │      - o → ⠕         │
     │                     │                             │                           │      - l → ⠇         │
     │                     │                             │                           │      - a → ⠁         │
     │                     │                             │                           │      - 123 → ⠼⠁⠃⠉   │
     │                     │                             │                           │                      │
     │                     │                             │  12. Retorna "⠠⠓⠕⠇⠁ ⠼⠁⠃⠉"                    │
     │                     │                             │<──────────────────────────│                      │
     │                     │                             │                           │                      │
     │                     │                             │  13. model.addAttribute() │                      │
     │                     │                             │      textoOriginal        │                      │
     │                     │                             │      resultadoBraille     │                      │
     │                     │                             │                           │                      │
     │                     │  14. Retorna "result"       │                           │                      │
     │                     │<────────────────────────────│                           │                      │
     │                     │                             │                           │                      │
     │                     │  15. Solicita template result.html                      │                      │
     │                     │─────────────────────────────────────────────────────────────────────────────>│
     │                     │                             │                           │                      │
     │                     │  16. result.html renderizado con datos                  │                      │
     │                     │<─────────────────────────────────────────────────────────────────────────────│
     │                     │                             │                           │                      │
     │  17. Muestra        │                             │                           │                      │
     │  resultado Braille  │                             │                           │                      │
     │<────────────────────│                             │                           │                      │
     │                     │                             │                           │                      │
```

## 2. Diagrama de Componentes y Dependencias

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         Spring Boot Application                         │
│                   ProyectoConstruccionApplication                       │
│                        @SpringBootApplication                           │
└──────────────────────┬──────────────────────────────────────────────────┘
                       │
                       │ Inicializa y gestiona
                       ▼
        ┌──────────────────────────────────────────┐
        │      Spring IoC Container                │
        │  (Inversión de Control y DI)             │
        └──────┬───────────────────┬────────────────┘
               │                   │
               │ Gestiona          │ Gestiona
               ▼                   ▼
    ┌──────────────────┐    ┌──────────────────┐
    │   Controllers    │    │    Services      │
    │   @Controller    │    │                  │
    └──────────────────┘    └──────────────────┘
               │                   │
               │ Depende de        │
               ▼                   ▼
    ┌──────────────────────────────────────┐
    │  TranscriptionController             │
    │  ┌────────────────────────────────┐  │
    │  │ - brailleMapper: BrailleMapper │  │
    │  │ + index(): String              │  │
    │  │ + transcribir(...): String     │  │
    │  └────────────────────────────────┘  │
    └────────────┬─────────────────────────┘
                 │
                 │ Utiliza
                 ▼
    ┌──────────────────────────────────────┐
    │         BrailleMapper                │
    │  ┌────────────────────────────────┐  │
    │  │ - map: Map<String, Integer>    │  │
    │  │ - SIGNO_NUMERO: int            │  │
    │  │ - SIGNO_MAYUSCULA: int         │  │
    │  │ + transcribir(String): String  │  │
    │  │ + maskToUnicode(int): String   │  │
    │  │ - initLetters(): void          │  │
    │  │ - initAccents(): void          │  │
    │  │ - initPunctuation(): void      │  │
    │  │ - initNumbers(): void          │  │
    │  └────────────────────────────────┘  │
    └──────────────────────────────────────┘
                 │
                 │ Genera
                 ▼
    ┌──────────────────────────────────────┐
    │    Braille Unicode Strings           │
    │    (U+2800 - U+28FF)                 │
    └──────────────────────────────────────┘
```

## 3. Flujo de Datos en BrailleMapper

```
Input: "Hola 123"
    │
    ▼
┌─────────────────────────────┐
│ 1. Normalizar espacios      │
│    replaceAll("\\s+", " ")  │
└──────────┬──────────────────┘
           ▼
   "Hola 123" (normalizado)
           │
           ▼
┌─────────────────────────────┐
│ 2. Iterar por cada carácter │
└──────────┬──────────────────┘
           │
           ├─────────► 'H' (Mayúscula)
           │             │
           │             ├─► Detectar mayúscula
           │             ├─► Añadir SIGNO_MAYUSCULA (⠠)
           │             └─► Convertir 'h' → ⠓
           │
           ├─────────► 'o' → map.get("o") → ⠕
           │
           ├─────────► 'l' → map.get("l") → ⠇
           │
           ├─────────► 'a' → map.get("a") → ⠁
           │
           ├─────────► ' ' → map.get(" ") → ⠀ (espacio)
           │
           ├─────────► '1' (Número)
           │             │
           │             ├─► Detectar dígito
           │             ├─► Si !inNumber: añadir SIGNO_NUMERO (⠼)
           │             ├─► inNumber = true
           │             └─► Convertir '1' → map.get("1") → ⠁
           │
           ├─────────► '2' (Número continuo)
           │             │
           │             └─► Convertir '2' → map.get("2") → ⠃
           │
           └─────────► '3' (Número continuo)
                       │
                       └─► Convertir '3' → map.get("3") → ⠉
           │
           ▼
┌─────────────────────────────┐
│ 3. Concatenar en StringBuilder│
└──────────┬──────────────────┘
           ▼
   Output: "⠠⠓⠕⠇⠁ ⠼⠁⠃⠉"
```

## 4. Mapeo de Caracteres Braille (Estructura de Datos)

```
BrailleMapper
│
├── map: HashMap<String, Integer>
│   │
│   ├── Letras Serie 1 (a-j)
│   │   ├── "a" → 0b000001 (⠁)
│   │   ├── "b" → 0b000011 (⠃)
│   │   ├── "c" → 0b001001 (⠉)
│   │   └── ...
│   │
│   ├── Letras Serie 2 (k-t)
│   │   ├── "k" → 0b000101 (⠅)
│   │   ├── "l" → 0b000111 (⠇)
│   │   └── ... (Serie 1 + punto 3)
│   │
│   ├── Letras Serie 3 (u-z)
│   │   ├── "u" → 0b100101 (⠥)
│   │   ├── "v" → 0b100111 (⠧)
│   │   └── ... (Serie 2 + punto 6)
│   │
│   ├── Vocales Acentuadas
│   │   ├── "á" → 0b101000 (⠨)
│   │   ├── "é" → 0b101000 (⠨)
│   │   └── ...
│   │
│   ├── Caracteres Especiales
│   │   ├── "ñ" → 0b111101 (⠻)
│   │   └── "ü" → 0b100101 (⠥)
│   │
│   ├── Puntuación
│   │   ├── "," → 0b000010 (⠂)
│   │   ├── "." → 0b110010 (⠲)
│   │   └── ...
│   │
│   └── Números (mapeo a serie a-j)
│       ├── "0" → map.get("j")
│       ├── "1" → map.get("a")
│       └── ...
│
├── SIGNO_MAYUSCULA = 0b101000 (⠠)
└── SIGNO_NUMERO = 0b111100 (⠼)

Conversión a Unicode:
    mask → char(0x2800 + mask) → Unicode Braille
```

## 5. Diagrama de Capas con Tecnologías

```
┌─────────────────────────────────────────────────────────────┐
│                      CAPA DE CLIENTE                        │
│                                                             │
│  Navegador Web (Chrome, Firefox, Edge, Safari)             │
│  • Renderiza HTML5                                          │
│  • Aplica CSS3                                              │
│  • Ejecuta JavaScript (si necesario)                        │
│  • Envía/Recibe peticiones HTTP                             │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       │ HTTP/HTTPS
                       ▼
┌─────────────────────────────────────────────────────────────┐
│                   CAPA DE PRESENTACIÓN                      │
│                                                             │
│  Thymeleaf 3.x (Template Engine)                            │
│  • Templates: index.html, result.html                       │
│  • Sintaxis: th:text, th:utext, th:href                     │
│  • Renderizado server-side                                  │
│  • CSS: styles.css                                          │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       │ Model & View
                       ▼
┌─────────────────────────────────────────────────────────────┐
│                     CAPA WEB (MVC)                          │
│                                                             │
│  Spring MVC                                                 │
│  • DispatcherServlet (Front Controller)                    │
│  • @Controller: TranscriptionController                     │
│  • @GetMapping, @PostMapping                                │
│  • Model: Datos para las vistas                            │
│  • ViewResolver: Resolución de templates                    │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       │ Llamadas de método
                       ▼
┌─────────────────────────────────────────────────────────────┐
│                   CAPA DE SERVICIO                          │
│                                                             │
│  Lógica de Negocio (POJOs)                                  │
│  • BrailleMapper                                            │
│  • Algoritmos de conversión                                 │
│  • Gestión de mapeos y reglas                              │
│  • Sin dependencias externas                                │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       │ Configuración y gestión
                       ▼
┌─────────────────────────────────────────────────────────────┐
│                 CAPA DE INFRAESTRUCTURA                     │
│                                                             │
│  Spring Boot 3.x + Java 21                                  │
│  • @SpringBootApplication                                   │
│  • IoC Container (BeanFactory)                              │
│  • Auto-configuration                                       │
│  • Embedded Tomcat 10.x                                     │
│  • Maven (pom.xml)                                          │
└─────────────────────────────────────────────────────────────┘
```

## 6. Patrones de Diseño Implementados

### 6.1 MVC (Model-View-Controller)
- **Model**: Datos (textoOriginal, resultadoBraille)
- **View**: Templates Thymeleaf (index.html, result.html)
- **Controller**: TranscriptionController

### 6.2 Dependency Injection
- Spring IoC Container gestiona las dependencias
- BrailleMapper se instancia en TranscriptionController

### 6.3 Front Controller
- DispatcherServlet actúa como punto único de entrada
- Enruta todas las peticiones HTTP

### 6.4 Strategy (implícito)
- BrailleMapper encapsula el algoritmo de conversión
- Fácilmente intercambiable por otras estrategias

### 6.5 Template Method
- Thymeleaf utiliza plantillas base
- Separación entre estructura y datos

---

**Documento elaborado por**: Grupo 7  
**Última actualización**: 24/11/2025  
**Versión**: 1.0

